schema_version: "1"
display_name: "Yeoul Memory"
short_description: "Use Yeoul CLI and memory rules safely in this repo."
default_prompt: "Use Yeoul proactively: prefer the user-level database at $HOME/.local/share/yeoul/work-memory.lbug for normal work, run preflight_briefing before non-trivial Yeoul work, record durable outcomes when clear, preserve provenance and lifecycle semantics, and prefer CLI workflows over raw storage queries."
